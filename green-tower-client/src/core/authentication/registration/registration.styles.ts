import { styled } from '@mui/material/styles';
import Typography from '@mui/material/Typography';

import { Config } from '../../../config/config';

export const RegistrationSubHeader = styled(Typography)`
  color: ${Config.colors.brown};
  font-weight: bold;
`;
